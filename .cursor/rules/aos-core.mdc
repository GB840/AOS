---
description: AOS 核心架构与命门哲学（始终生效）
alwaysApply: true
---

# AOS 核心（宪法摘要，全文见根目录 AGENTS.md）

AOS = 单内核 FabricHub + 多芯粒的 agent 运行时。价值在「多步编排 + 故障隔离 + 统一路由 + 端云合作」，不是模型多聪明。

## 命门哲学（违反即"做歪了"）
1. 万物为我所用：引擎/模型/工具皆可插拔供给方，核心不绑定任何一家。
2. 端云合作：同一能力多供给方，**云端优先→本地兜底**是路由机制，不是口号。
3. 一样用不了就换别样：云端挂自动降级本地，一源失败自动跳下一个。
4. 不绑定、零成本：搜索/记忆/推理/绘图全有开源本地方案。**永远不要因"要花钱"放弃能力**，付费 key 只是可选增强。

## 真架构（勿按 legacy 理解）
- FabricHub（`src/kernel/plugins/fabric_hub.py`）是唯一内核，管路由/记忆/上下文主权。
- 芯粒＝隔离计算单元，重型引擎跑独立子进程，崩溃不传染。
- 按 Capability 发现引擎不按名字（`src/core/fabric/capability.py`、`registry.py`）。
- 所有适配器实现 `BaseAgentAdapter`（`src/core/fabric/adapter.py`）。
- 对外＝标准 MCP server（`src/mcp/protocol.py`）。

## 反模式（见到就改）
- 路由只取 providers[0] 就停 → 必须遍历所有 live 供给方故障转移。
- 能力焊死付费 key → mem0 默认 AOS_MEM0_LOCAL=1 走本地。
- 虚报能力 → advertise 只声明真能干的。
- 往 legacy 双轨（brain/deerflow/swarm/hermes）加料 → 新功能一律进 FabricHub。

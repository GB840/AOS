# =============================================================================
# 已废弃 —— 依赖真相见 pyproject.toml [project].dependencies
# 本文件仅作向后兼容保留（verify_setup.py / 旧文档仍引用它）。
# 新环境请用:  uv pip install -e ".[dev,test]"
# =============================================================================
# 单创OS核心依赖
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# AI 编排引擎（真正开源库）
langgraph>=1.0.0
langchain-core>=1.0.0
crewai>=1.0.0

# 本地大模型（可选，根据硬件配置安装）
# transformers>=4.30.0
# torch>=2.0.0
# llama-cpp-python>=0.2.0

# 数据与工具
numpy>=1.24.0
PyYAML>=6.0
requests>=2.30.0
